[project]
name = "livelaucher"
version = "1.0.1"
description = "Tkinter-based sequential launcher with port verification"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.1",
]

[project.scripts]
livelaucher = "main:main"
